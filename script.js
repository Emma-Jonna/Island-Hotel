const body = document.querySelector('body');
const tableHead = document.querySelectorAll('thead');
const tableBody = document.querySelectorAll('tbody');
const tableRow = document.querySelectorAll('tr');
const tableHeader = document.querySelectorAll('th');
const days = document.querySelectorAll('td');
const arrivalDay = document.getElementById('arrival');
const departureDay = document.getElementById('departure');

/* arrivalDay.addEventListener('change', () => {
  console.log(arrivalDay.value);
});

departureDay.addEventListener('change', () => {
  console.log(departureDay.value);
}); */
